# Deploy Queue
# This file is the ONLY queue the cron reads. Roger writes here; cron executes and clears.
# Format: one item per bullet. Delete bullet after completion.

<!-- EMPTY — all items from Apr 28 executed manually -->
